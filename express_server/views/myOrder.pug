extends layout
block content
    header Orders List
    table.table-container
        tr
            th Order
            th date
            th totalPrice
        each order, index in orders
            tr
                td= index+1
                td= order.date.toISOString().split('T')[0]
                td= order.totalPrice
